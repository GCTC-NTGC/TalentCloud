{# =============================================================================

    Talent Cloud
    Applicant: Job Poster - Education & Criteria

============================================================================= #}
<section id="criteria">
    <h3 data-c-heading="h3" data-c-margin="bottom(1)" data-c-padding="top(3) bottom(1)">{{ job_post.criteria.title }}</h3>
    <h4 data-c-heading="h4" data-c-margin="top(2) bottom(1)">{{ job_post.criteria.essential_title }}</h4>
    {% if criteria.essential is defined and criteria.essential is not empty %}
        <ul>
            {% for criterion in criteria.essential %}
                <li data-c-margin="bottom(1)">
                    <p data-c-font-weight="bold" data-c-margin="bottom(.5)">{{ criterion.skill.name }}</p>
                    <p data-c-margin="bottom(.5)">{{ criterion.description ? criterion.description|nl2br : criterion.skill.description|nl2br }}</p>
                    <p>{{ criterion.specificity|nl2br }}</p>
                </li>
            {% endfor %}
        </ul>
    {% else %}
        <p>{{ job_post.criteria.no_info }}</p>
    {% endif %}
    {% if criteria.asset != false %}
        <h4 data-c-heading="h4" data-c-margin="top(2) bottom(1)">{{ job_post.criteria.asset_title }}</h4>
        {% if criteria.asset is defined and criteria.asset is not empty %}
            <ul>
                {% for criterion in criteria.asset %}
                    <li data-c-margin="bottom(1)">
                        <p data-c-font-weight="bold" data-c-margin="bottom(.5)">{{ criterion.skill.name }}</p>
                        <p data-c-margin="bottom(.5)">{{ criterion.description ? criterion.description|nl2br : criterion.skill.description|nl2br }}</p>
                        <p>{{ criterion.specificity|nl2br }}</p>
                    </li>
                {% endfor %}
            </ul>
        {% else %}
            <p>{{ job_post.criteria.no_info }}</p>
        {% endif %}
    {% endif %}
</section>
