{# =============================================================================

    Talent Cloud
    Applicant: Profile - Skills Index

============================================================================= #}
<section
    class="applicant-profile__portfolio">

    <div
        class="container--layout">

        {# Profile List (Soft Skills) #}

            <div
                class="profile-list">

                {# Profile List Header #}

                    <div
                        class="profile-list__header flex-grid middle">

                        <div
                            class="box med-1of2">

                            <h3>{{ profile.skills_section.soft_title }}</h3>

                        </div>

                        <div
                            class="box med-1of2">

                            <button
                                class="profile-list__add-element-trigger">
                                {{ profile.skills_section.add_button_label }}
                            </button>

                        </div>

                    </div>

                {# Set Locale #}

                    {% set locale = "profile" %}

                {# Set Type #}

                    {% set skillType = "soft" %}

                {# Profile Element Template (Sample) #}

                    {% include "common/skill" %}

                {# Profile Element List #}

                    <div 
                        class="profile-element-list">

                        {% set softSkillCounter = 0 %}

                        {% for skill in user.skills %}

                            {% for siteSkill in skills %}

                                {% if siteSkill.type == "soft" %}

                                    {% if skill.name|capitalize == siteSkill.name|capitalize %}

                                        {% set softSkillCounter = softSkillCounter + 1 %}

                                        {% include "common/skill" %}

                                    {% endif %}

                                {% endif %}

                            {% endfor %}

                        {% endfor %}

                        <div
                            class="profile-null {% if softSkillCounter == 0 %}active{% endif %}">

                            <p>{{ profile.skills_section.null_copy }}</p>

                        </div>

                    </div>

            </div>

        {# Profile List (Hard Skills) #}

            <div
                class="profile-list">

                {# Profile List Header #}

                    <div
                        class="profile-list__header flex-grid middle">

                        <div
                            class="box med-1of2">

                            <h3>{{ profile.skills_section.hard_title }}</h3>

                        </div>

                        <div
                            class="box med-1of2">

                            <button
                                class="profile-list__add-element-trigger">
                                {{ profile.skills_section.add_button_label }}
                            </button>

                        </div>

                    </div>

                {# Set Locale #}

                    {% set locale = "profile" %}

                {# Set Type #}

                    {% set skillType = "hard" %}

                {# Profile Element Template (Sample) #}

                    {% include "common/skill" %}

                {# Profile Element List #}

                    <div 
                        class="profile-element-list">

                        {% set hardSkillCounter = 0 %}

                        {% for skill in user.skills %}

                            {% for siteSkill in skills %}

                                {% if siteSkill.type == "hard" %}

                                    {% if skill.name|capitalize == siteSkill.name|capitalize %}

                                        {% set hardSkillCounter = hardSkillCounter + 1 %}

                                        {% include "common/skill" %}

                                    {% endif %}

                                {% endif %}

                            {% endfor %}

                        {% endfor %}

                        <div
                            class="profile-null {% if hardSkillCounter == 0 %}active{% endif %}">

                            <p>{{ profile.skills_section.null_copy }}</p>

                        </div>

                    </div>

            </div>

    </div>

</section>
