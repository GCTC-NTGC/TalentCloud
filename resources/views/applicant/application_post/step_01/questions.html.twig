{# =============================================================================

    Talent Cloud
    Applicant: Application Post - Questions

============================================================================= #}
<div
    class="application-post__questions">

    <div
        class="container--copy">

        <h3>
            {{ application.question_title }}
        </h3>

        {% for question in job.questions %}

            <span>
                {{ question.value }}
            </span>

            <p>
                {{ question.description }}
            </p>

            <div 
                class="form__input-wrapper--float {% if question.answer %}active{% endif %}">
                <label 
                    class="form__label" 
                    for="{{ question.input_name }}">
                    {{ question.answer_label }}
                </label>
                <textarea
                    class="form__textarea"
                    name="{{ question.input_name }}">{% if question.answer != null %}{{ question.answer }}{% endif %}</textarea>
            </div>

        {% endfor %}

        <div
            class="flex-grid middle application-post__action-wrapper">

            <div 
                class="box med-1of2">

                <button
                    class="button--blue light-bg">
                    {{ application.save_quit_button_label }}
                </button>

            </div>

            <div 
                class="box med-1of2">

                <button
                    class="button--blue light-bg">
                    {{ application.save_continue_button_label }}
                </button>

            </div>

        </div>

    </div>

</div>
