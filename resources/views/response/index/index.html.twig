{# =============================================================================

    Talent Cloud
    COVID-19 Response
    Purpose: This page is to help locate talent that is dedicated to helping the
    relief effort for the COVID-19 pandemic.

============================================================================= #}
{% extends "layouts/base-h2" %}
{% block title %}{{ response.page_title }}{% endblock %}
{% block body %}
    {# Government of Canada Banner #}
    {% include "common/goc" %}
    {% if is_internet_explorer == true and app.request.cookies.get('browser_message') == false %}
        {% include "common/h2-ie-banner" %}
        {% include "common/h2-ie-dialog" %}
    {% endif %}
    <div data-clone>
        {# Beta Banner #}
        {% include "response/beta-banner" %}
        {# Menu #}
        {% include "response/menu" %}
        {# Header #}
        {% include "response/index/header" %}
        {# Content #}
        {% include "response/index/content" %}
        {# Job List #}
        {% include "response/index/job-list" %}
        {# How to Apply #}
        {% include "response/index/how" %}
    </div>
{% endblock %}

{#
    - remove levels everywhere (poster, application, manager tracking)
    - comms, manager, strat comms (example poster)
    - turn off soft skills
#}
