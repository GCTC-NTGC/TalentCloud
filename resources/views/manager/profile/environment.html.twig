{# =============================================================================

    Talent Cloud
    Manager: Profile - About Me

============================================================================= #}
<a
    id="environment">
</a>

<section
    id="managerProfileSectionEnvrionment"
    class="manager-profile__section">

    <h4
        class="manager-profile__section-title">
        Our Work Environment
    </h4>

    <fieldset
        class="form__fieldset">

        <legend
            class="form__legend">
            Things to Know
        </legend>

        <div
            class="flex-grid">

            <div
                class="box med-1of2">

                <div class="form__input-wrapper--float {% if work_environment.translate('en').things_to_know %}active{% endif %}">
                    <label class="form__label" for="managerProfileEnvContextEN">
                        Answer (English)
                    </label>
                    <textarea
                        class="form__textarea"
                        id="managerProfileEnvContextEN"
                        name="things_to_know[en]">
                        {{ work_environment.translate('en').things_to_know }}
                    </textarea>
                </div>

            </div>

            <div
                class="box med-1of2">

                <div class="form__input-wrapper--float {% if work_environment.translate('fr').things_to_know %}active{% endif %}">
                    <label class="form__label" for="managerProfileEnvContextFR">
                        Answer (Français)
                    </label>
                    <textarea
                        class="form__textarea"
                        id="managerProfileEnvContextFR"
                        name="things_to_know[fr]">
                        {{ work_environment.translate('fr').things_to_know }}
                    </textarea>
                </div>

            </div>

            <div
                class="box full">

                <div
                    aria-orientation="horizontal"
                    class="form__radio-group"
                    role="radiogroup">

                    <label
                        class="form__label"
                        for="">
                        How Often is Telework Allowed?
                    </label>

                    <div class="flex-grid">

                        {% set counter = 0 %}

                        {% for option in telework_options %}

                            {% set counter = counter + 1 %}

                            <label
                                class="box form__radio-group-label small-1of{{ telework_options|length }}"
                                for="telework{{ option.id }}">
                                <span
                                    class="hidden">
                                    {{ profile.work_environment.telework_allowed[option.name] }}
                                </span>
                                <input
                                    class="form__radio-group-input"
                                    id="telework{{ option.id }}"
                                    name="telework"
                                    type="radio"
                                    value="{{ option.id }}"
                                    {% if work_environment.telework_allowed_frequency.name == option.name %}
                                        checked
                                    {% endif %} />
                                <span
                                    class="form__radio-group-span">
                                    {{ profile.work_environment.telework_allowed[option.name] }}
                                </span>
                            </label>

                        {% endfor %}

                    </div>

                </div>

            </div>

            <div
                class="box full">

                <div
                    aria-orientation="horizontal"
                    class="form__radio-group"
                    role="radiogroup">

                    <label
                        class="form__label"
                        for="">
                        How Often Are Flexible Hours Allowed?
                    </label>

                    <div class="flex-grid">

                        {% set counter = 0 %}

                        {% for option in flex_hour_options %}

                            {% set counter = counter + 1 %}

                            <label
                                class="box form__radio-group-label small-1of{{ flex_hour_options|length }}"
                                for="flex_hours{{ option.id }}">
                                <span
                                    class="hidden">
                                    {{ profile.work_environment.flex_hours_allowed[option.name] }}
                                </span>
                                <input
                                    class="form__radio-group-input"
                                    id="flex_hours{{ option.id }}"
                                    name="flex_hours"
                                    type="radio"
                                    value="{{ option.id }}"
                                    {% if work_environment.flexible_hours_frequency.name == option.name %}
                                        checked
                                    {% endif %} />
                                <span
                                    class="form__radio-group-span">
                                    {{ profile.work_environment.flex_hours_allowed[option.name] }}
                                </span>
                            </label>

                        {% endfor %}

                    </div>

                </div>

            </div>

            <div
                class="box full">
                <h4
                    class="manager-profile__legend">
                    Work Environment Photos
                </h4>
            </div>

            {% for photo in user.workplace_photos %}

                <div
                    class="box med-1of3">

                    <div
                        class="manager-profile__image-upload">
                        <img
                            {% if photo.image != null %}
                                src="{{ photo.url }}"
                            {% else %}
                                src="/images/user.png"
                            {% endif %}>
                    </div>

                    <div class="form__input-wrapper--upload">
                        <label class="form__label" for="managerEnvPhoto01">
                            Upload an Image
                            <input
                                class="form__input"
                                id="managerEnvPhoto01"
                                type="file"
                                name="workplace_photos[{{ photo.id }}]"
                                value="">
                            </input>
                        </label>
                    </div>

                    <div class="form__input-wrapper--float {% if photo.alt != null %}active{% endif %}">
                        <label class="form__label" for="manageProfileAltEN{{ photo.id }}">
                            Image Alt Text (English)
                        </label>
                        <input
                            class="form__input"
                            id="manageProfileAltEN{{ photo.id }}"
                            type="text"
                            name="workplace_photo_captions[{{ photo.id }}][en]"
                            value="{{ photo.alt }}">
                        </input>
                    </div>

                    <div class="form__input-wrapper--float {% if photo.alt_fr != null %}active{% endif %}">
                        <label class="form__label" for="manageProfileAltFR{{ photo.id }}">
                            Image Alt Text (Français)
                        </label>
                        <input
                            class="form__input"
                            id="manageProfileAltFR{{ photo.id }}"
                            type="text"
                            name="workplace_photo_captions[{{ photo.id }}][fr]"
                            value="{{ photo.alt_fr }}">
                        </input>
                    </div>

                </div>

            {% endfor %}

        </div>

    </fieldset>

    <div
        class="flex-grid">

        <div
            class="box med-1of2">

        </div>

        <div
            class="box med-1of2">
            <button
                class="button--blue light-bg"
                type="submit"
                name="submit"
                value="work_environment">
                Save Our Work Environment
            </button>
        </div>

    </div>

</section>
