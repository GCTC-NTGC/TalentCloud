# TalentCloud codecov.io config
# .codecov.yml

coverage:
  range: 30..70
  round: up
  status:
    patch: no
    project:
      default:
        base: auto
        target: auto
        threshold: 5%
        branches: null
        only_pulls: false

comment:
  layout: "reach, files, diff"
