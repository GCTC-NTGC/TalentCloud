# TalentCloud codecov.io config
# .codecov.yml

coverage:
  range: 25..75
  round: up
  status:
    project:
      default:
        base: auto
        target: auto
        threshold: null
        branches: null
      patch: no

comment:
  layout: "header, diff, tree, changes"
  behavior: default
