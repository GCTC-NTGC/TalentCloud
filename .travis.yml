language: php

php:
  - 7.0

sudo: required

services:
  - docker
  - postgresql

addons:
  postgresql: "9.6"

cache:
  directories:
    - vendor

script:
  - cp -a .env.example .env
  - make gen-certs
  - docker-compose up -d
  - make set-root-perms
  - make composer-install
  - php artisan key:generate
  - make build-db

after_script:
  # make phpunit
  - mkdir shared
  - docker volume create sharedir
  - docker run -d -v /var/www/shared:/var/lib/docker/volumes/sharedir/_data talentcloud_talentcloud:latest
  - docker-compose exec -T talentcloud vendor/bin/phpunit --coverage-text=coverage.txt
  - docker cp coverage.txt shared
  - docker cp talentcloud:/coverage.txt /var/lib/docker/volumes/sharedir/_data
  - docker exec talentcloud sh -c "mv coverage.txt /var/lib/docker/volumes/sharedir/_data/coverage.txt"
  - wget https://scrutinizer-ci.com/ocular.phar
  - php ocular.phar code-coverage:upload --format=php-clover coverage.xml
  - make docker-stop

after_success:
  - bash <(curl -s https://codecov.io/bash)
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
  - docker tag talentcloud_talentcloud:latest gctcntgc/talentcloud:travis
  - docker push gctcntgc/talentcloud:travis

# Slack notifications / do not send emails
notifications:
  email: never
  slack: talent-cloud:ASWQRTtW3oT2C2DHYaBzSCJg
  on_success: change
  on_failure: always
